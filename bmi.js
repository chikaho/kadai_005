// 変数の宣言
let userWeight;
let userHeight;

//値の代入
userWeight = '体重';
userHeight = '身長';

// 変数の宣言・値の代入
let userWeightnum = 68;
let userHeightnum = 1.7;

// コンソールへの出力
console.log(userWeightnum / (userHeightnum * userHeightnum));